import type {ReactNode} from 'react';
import './style.css';
export const metadata={title:'NEXUS · Community Growth OS',description:'See where newcomers drop off. Fix it. Measure what worked.'};
export default function Layout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>;}
