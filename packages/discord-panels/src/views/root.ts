import {ButtonStyle} from 'discord-api-types/v10';
import {actionRow,callout,divider,footer,nexusPanel,type Panel} from '../primitives.js';
import type {Issue} from '../types.js';
export async function rootPanel(issue:Issue):Promise<Panel>{return nexusPanel({title:'NEXUS · Community Control Center',subtitle:'Discord-native community activation and experimentation',children:[divider(),callout('Operating loop','OBSERVE → MEASURE → DIAGNOSE → INTERVENE → EXPERIMENT → MEASURE AGAIN'),callout('Start here','Overview gives the executive snapshot. Lifecycle reveals drop-offs. Experiments measure whether interventions improve outcomes.'),footer('Administration opens privately. Production metrics exclude tests and previews.')],rows:[await actionRow(issue,[{label:'Overview',action:'overview',style:ButtonStyle.Primary,publicEntry:true},{label:'Lifecycle',action:'lifecycle',publicEntry:true},{label:'Diagnose',action:'diagnose',publicEntry:true},{label:'Experiments',action:'experiments',publicEntry:true}]),await actionRow(issue,[{label:'Setup',action:'setup',publicEntry:true},{label:'Activation',action:'activation',publicEntry:true},{label:'Interventions',action:'interventions',publicEntry:true},{label:'Settings',action:'advanced',publicEntry:true}]),await actionRow(issue,[{label:'Cohorts',action:'cohorts',publicEntry:true},{label:'Reports',action:'reports',publicEntry:true},{label:'Privacy',action:'privacy',publicEntry:true},{label:'Billing',action:'billing',publicEntry:true}])]});}
