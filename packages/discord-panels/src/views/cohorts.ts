import {actionRow,callout,divider,footer,nexusPanel,type Panel} from '../primitives.js';
import type {Issue} from '../types.js';
export async function cohortsPanel(issue:Issue):Promise<Panel>{return nexusPanel({title:'NEXUS · Cohorts',subtitle:'Comparable member groups and mature windows',children:[divider(),callout('Membership episodes','Metrics use membership episodes so rejoining members are measured in the correct cohort.'),callout('Mature observation windows','Members remain provisional until the full metric window has elapsed.'),callout('Version pinning','Activation and onboarding definitions stay attached to the cohort that experienced them.'),footer('Advanced cohort editing is not enabled in this rollout.')],rows:[await actionRow(issue,[{label:'Lifecycle',action:'lifecycle'},{label:'Overview',action:'overview'}])]});}
